/**
 * PSTO desk sidebar — shared chrome via CommandSidebar (role-aware nav).
 * Layout structure matches command-style sidebars; theme colors unchanged.
 */
import CommandSidebar from '@/components/layout/CommandSidebar';

const PstoSidebar = () => {
    return <CommandSidebar />;
};

export default PstoSidebar;
